package com.collection.stream;

import java.util.ArrayList;
import java.util.List;

public class StreamDemo1 {

	
	public static void main(String[] args) {
		
		List<String> names = new ArrayList<String>();
		names.add("raj");
		names.add("jay");
		names.add("amit");
		names.add("kunal");
		names.add("shyam");
		names.add("parth");
		
		
		//names.stream().forEach(x->System.out.println(x));
		//names.stream().map(x->x.toUpperCase()).forEach(x->System.out.println(x));
		//names.stream().filter(x->x.length()>3).forEach(x->System.out.println(x));
		//names.stream().filter(x->x.length()>3).map(x->x.toUpperCase()).forEach(x->System.out.println(x));
		
		
		
	}
}
